const calcArea = (base: number, altura: number) => {
    return base * altura;
}

console.log(calcArea(30, 20));


const saudacao = (nome: string) => {
    return 'Olá, ' + nome + '!';
}

console.log(saudacao('EBAC'));
